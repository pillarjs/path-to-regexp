{
  "name": "path-to-regexp",
  "description": "Express style path to RegExp utility",
  "version": "0.0.2",
  "keywords": ["express", "regexp"],
  "component": {
    "scripts": {
      "path-to-regexp": "index.js"
    }
  }
}
